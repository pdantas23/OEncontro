/**
 * src/hooks/useAuth.ts
 *
 * Re-exporta o hook useAuth do AuthProvider para uso nos componentes.
 * Importar sempre deste módulo — não importar diretamente do AuthProvider.
 */

export { useAuth } from '@/providers/AuthProvider'
